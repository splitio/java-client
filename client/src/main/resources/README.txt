This is a collection of sample datasets that every SDK should test against in order to ensure compatibility

What is sample-data.csv?

Given the seed and key as input, what should be the hash value returned by the hash function and the corresponding bucket (value in range 1 to 100 inclusive) that should be returned by the sdk

